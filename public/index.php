<?php
    // import application from the app directory
    /**
     *  in the app directory there is a bootstrap.php file
     *  
     * */
    require_once('../app/bootstrap.php');

    /**
     *  in web application there is a CORE which handles url,
     * */
    $init = new Core;